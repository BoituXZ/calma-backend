# Requirements for Calma AI Training with Hugging Face Dataset
# Install with: pip install -r requirements-training.txt

# Core ML frameworks
torch>=2.0.0
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.25.0

# LoRA and quantization
peft>=0.10.0
bitsandbytes>=0.41.0

# Hugging Face Hub
huggingface-hub>=0.20.0

# Utilities
numpy>=1.24.0
scipy>=1.10.0
tqdm>=4.65.0

# Optional but recommended
# wandb  # For experiment tracking (uncomment if needed)

# For inference server (if not already installed)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
